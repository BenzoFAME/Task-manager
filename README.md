Task Manager Var1 🗂️

Простое веб-приложение для управления задачами с поддержкой категорий, подзадач, комментариев, прикрепления файлов и архивации задач. Построено на Spring Boot, Thymeleaf, Spring Data JPA и MySQL.

---

Функционал:

- Управление задачами:
  - Создание, редактирование и удаление задач.
  - Установка приоритета (LOW, MEDIUM, HIGH) и статуса (TODO, IN_PROGRESS, COMPLETED).
  - Установка срока выполнения и отображение таймера обратного отсчета.
- Категории:
  - Создание и удаление категорий.
  - Фильтрация задач по категориям.
- Подзадачи:
  - Создание, удаление и отметка выполнения подзадач.
  - Автоматический расчёт прогресса выполнения задачи.
- Комментарии:
  - Добавление и удаление комментариев к задачам.
- Файлы:
  - Прикрепление и скачивание файлов к задачам.
- Архив:
  - Архивация и восстановление задач.
- Фильтры и сортировка:
  - По приоритету, статусу, дедлайну, ключевому слову и просроченным задачам.

---

Структура проекта:

src/main/java/com/example/taskmanagervar/
  ├─ controller/       # Контроллеры (Task, Archive, Category, Comment, Subtask)
  ├─ service/          # Сервисы (логика работы с сущностями)
  ├─ model/            # Сущности: Task, Category, Subtask, Comment, TaskFile
  ├─ repository/       # Репозитории для работы с БД

src/main/resources/
  ├─ templates/        # Шаблоны Thymeleaf (index, task-view, task-edit, archive)
  └─ uploads/          # Загруженные файлы (не в Git)

---

Используемые технологии:

- Java 17+
- Spring Boot 3
- Thymeleaf
- Spring Data JPA / Hibernate
- MySQL
- Bootstrap 5
- Maven

---

Установка и запуск:

1. Клонировать репозиторий:
   git clone https://github.com/BenzoFAME/Task-manager.git
   cd Task-manager

2. Создать базу данных MySQL:
   CREATE DATABASE taskmanager;

3. Настроить application.properties:
   spring.datasource.url=jdbc:mysql://localhost:3306/taskmanager
   spring.datasource.username=root
   spring.datasource.password=YOUR_PASSWORD
   spring.jpa.hibernate.ddl-auto=update

4. Запустить приложение:
   mvn spring-boot:run

5. Открыть приложение в браузере:
   http://localhost:8080

---

Особенности реализации:

- Используется REST-подход для форм, все действия через POST-запросы.
- Подзадачи и комментарии добавляются динамически на странице с помощью Thymeleaf и JavaScript.
- Автоматическое создание папки uploads/ для прикрепленных файлов.
- Архивированные задачи исключаются из основной панели, но доступны в отдельном разделе.

---

Автор:

BenzoFAME
GitHub: https://github.com/BenzoFAME

